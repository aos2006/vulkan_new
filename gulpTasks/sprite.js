const gulp = require('gulp'),      l = require('gulp-load-plugins')(),      sprite = require('gulp.spritesmith'),      combine = require('stream-combiner2').obj;module.exports = function(options){  return function () {    var spriteData;    return combine(      gulp.src('src/img/icons-png/*.png'),        sprite({        imgName: 'png-sprite.png',        cssName: '_png-sprite.sass',        cssFormat: 'sass',        algorithm: 'binary-tree',      }),    l.if(sprite.img,  gulp.dest('build/img/')),    l.debug({title: 'sprite-images:create'}),    l.if(sprite.css, gulp.dest('src/sass/helpers/')),    l.debug({title: 'sprite-sass:create'})    );  }};